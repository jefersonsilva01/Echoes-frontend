import styled from "styled-components";

const SearchResultsStyles = styled.section`
  width: 100%;
  height: auto;
  background-color: var(--WT-Main);
  padding: 40px 16px;
  position: relative;
  top: 60px;
`;

export { SearchResultsStyles };